# VIGIL AI — minimal CPU dependencies for Windows 11 + Python 3.11.9
# Verified clean install 2026-08-29: Python 3.11.9, torch 2.3.1+cpu, torchaudio 2.3.1+cpu, numpy 1.26.4, soundfile 0.14.0
# Matched pair 2.3.1+cpu is genuinely compatible on Windows CPU and supports AASIST (15 tests PASS).
# Previous host had mismatched torch 2.13.0+cpu / torchaudio 2.11.0+cpu (minor mismatch, Resample still worked).
# Attempted torch 2.4.0+cpu failed to load fbgemm.dll (OSError 126) on this host, so reverted to 2.3.1.
# For fresh install, use CPU index: pip install -r ai/requirements.txt --index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchaudio==2.3.1
soundfile==0.14.0
numpy==1.26.4
# Optional but useful for mp3/m4a fallback
# librosa not required — torchaudio + soundfile cover wav/flac/mp3/ogg
